# Workspace venv requirements for the memory subsystem.
# Pinned 2026-04-30 after Bob asked for memory_search MCP fixes.
# Install:  ~/.openclaw/workspace/venv/bin/pip install -r requirements-memory.txt

sentence-transformers>=5.3.0
torch>=2.4.0
numpy>=2.0
pyarrow>=18.0.0
lancedb>=0.20.0
