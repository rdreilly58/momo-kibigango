apiVersion: 1

policies:
  - orgId: 1
    receiver: Telegram
    group_by: ['alertname', 'severity']
    group_wait: 30s
    group_interval: 5m
    repeat_interval: 4h
    routes:
      - receiver: Telegram
        matchers:
          - severity =~ "critical|warning"
        continue: false
