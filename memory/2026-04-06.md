# Memory — April 6, 2026

## CRITICAL BEHAVIOR CHANGE (05:36 EDT) — ENFORCED GOING FORWARD

Bob explicitly instructed: **Provide proactive status updates on ALL multi-step and long-running tasks WITHOUT being prompted.**
- This applies to every request and interaction going forward
- No need for Bob to ask "status?" — Momotaro volunteers updates
- Announce steps, progress, results, and blockers proactively
- Applies to: investigations, subagent tasks, file reads, config changes, everything

---

## Rocket.Chat Investigation (Mon Apr 6, 2026 — IN PROGRESS)

**Symptom:** Bob sees "LLM error" in Rocket.Chat from work computer.

**Root cause found in gateway.log:**
- The Rocket.Chat channel (`[rocketchat] [default]`) has been in a **continuous restart loop since April 2, 2026**
- Pattern: starts → fails → auto-restart attempt 1/10 → ... → 7/10 → health-monitor restarts → repeat
- This has been happening for **days** — not a new issue
- The channel never successfully connects/stays up

**Key log observations:**
- `[rocketchat] [default] starting Rocket.Chat channel` immediately followed by `auto-restart attempt X/10`
- No successful connection logged — immediately fails each time
- Health monitor keeps resetting the counter (reason: stopped)
- The plugin IS registered: `[plugins] acpx runtime backend registered`
- Gateway model: `anthropic/claude-opus-4-6` (at most restart points)

**Bob's hint:** The Telegram channel fix required a change in how we configure channels. Study that fix for possible insights.

**Memory note:** April 2 MEMORY.md entry says "RC is working! Bob can message from work computer via Rocket.Chat" — but the logs show it was already failing that same day (restart loop started April 2 at 13:50).

**Next steps (NOT YET COMPLETED):**
1. Find the Telegram channel fix details
2. Review RC plugin config (`~/.openclaw/extensions/openclaw-channel-rocketchat/`)
3. Check OpenClaw config for RC channel settings
4. Apply fix

---

## Session Context

- Current model: `anthropic/claude-sonnet-4-6`
- Bob is at work (Leidos), messaging via Telegram
- Bob is on work computer trying to use Rocket.Chat
- Previous agent run was aborted by user (05:44 EDT)
